Save current progress to GitHub.

1. Check git status to see what changed
2. Stage all changes (except secrets/data files per .gitignore)
3. Create a commit with a summary of changes
4. Push to origin

Ask for a brief commit message if the changes are significant, otherwise auto-generate one.
