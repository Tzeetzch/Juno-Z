Build the project and check for errors.

1. Run `dotnet build` in the solution directory
2. Report: success or failure
3. If errors: list them clearly and suggest fixes
4. If warnings: mention them but don't block
