Run all tests and report results.

1. Run `dotnet test` in the solution directory
2. Summarize: how many passed, failed, skipped
3. If failures: show the failing test names and brief error descriptions
4. Suggest fixes for any failures
